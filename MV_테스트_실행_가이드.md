# MV 테스트 실행 가이드

작성일: 2026-05-06

## 목적

MV 개선 작업 후 기존 기능이 깨지지 않았는지 빠르게 확인하기 위한 안내서입니다.  
개선 전후 비교, 오류 발생 시 원인 추적, 다음 단계 작업 전 안전 확인에 사용합니다.

## 실행 명령

프로젝트 폴더에서 아래 명령을 실행합니다.

```text
npm run test:mv
```

프로젝트 위치:

```text
/Users/leemunhyeong/Music Creator
```

## 정상 결과

정상 통과 시 마지막 줄에 아래와 비슷한 결과가 표시됩니다.

```text
MV smoke test suite: PASS (57 checks)
```

이 결과는 MV 관련 핵심 보호 테스트가 모두 통과했다는 뜻입니다.

## 현재 보호 범위

현재 통합 테스트는 아래 항목을 확인합니다.

1. `app.js`, `js/step6.js` 문법 오류 여부
2. MV 테스트 파일 문법 오류 여부
3. MV 전용 데이터 모델 저장/복원 동작
4. MV 설정 저장/불러오기와 이미지 수 계산 동작
5. MV 장소 태그의 영어/한글 문자열 변환과 씬 가사 기반 장소 선택 동작
6. MV 캐릭터 시트 토글, 복사, 요약/전체 텍스트 추출 동작
7. MV 캐릭터 입력 UI 생성과 기존 값/시트 복원 동작
8. `app.js`와 `js/step6.js` 사이의 중복 전역 함수 재발 여부
9. Chrome 실제 런타임에서 MV 핵심 전역 함수와 화면 요소 로딩 여부
10. MV 프롬프트 저장, 섹션 복사, 간단 번역 기능
11. MV 프롬프트 TXT 다운로드 기능
12. SRT 자막 복사와 다운로드 기능
13. MV 프롬프트 전체 복사 기능
14. 씬 프롬프트 개별 저장 기능
15. 태그 버튼 중복 이벤트 방지
16. 캐릭터 시트 생성 성공/입력 가드/API 키 가드 보호
17. 썸네일, 스타일, 씬, 씬 개요 프롬프트 재생성 보호
18. 한글/영어 프롬프트 동기화 보호
19. 씬 개요 확정 후 결과 화면 렌더링 보호
20. 프로젝트 재진입 시 6단계 마케팅/MV 데이터 복원 보호
21. `js/step6.js` 내부 섹션 경계와 테스트용 보호 마커 유지 여부
22. `marketing.mv.scenes` 정규화 모델과 기존 씬 데이터 호환

## 실패했을 때 확인 순서

테스트가 실패하면 먼저 마지막에 표시된 실패 파일명을 확인합니다.

예시:

```text
▶ Run: tests/mv_regenerate_scene_prompt_smoke.js
```

이 경우 `tests/mv_regenerate_scene_prompt_smoke.js`에서 검증하는 기능이 실패한 것입니다.

확인 순서는 다음과 같습니다.

1. 실패한 테스트 파일명 확인
2. 바로 위에 표시된 오류 메시지 확인
3. 최근 변경한 파일이 `app.js`, `js/step6.js`, `js/storage.js` 중 어디인지 확인
4. `MV_개선작업_이력.md`에서 마지막 성공 작업 기록 확인
5. 필요하면 Git branch 또는 백업 폴더 기준으로 이전 상태와 비교

## Chrome 런타임 테스트 참고

`tests/mv_chrome_runtime_check.js`는 실제 Chrome headless 런타임을 사용합니다.  
이 테스트는 임시 로컬 서버를 띄운 뒤 자동으로 종료합니다.

정상적인 경우 별도 서버를 수동으로 켤 필요가 없습니다.

## 개선 작업 전 권장 절차

다음 개선 작업을 시작하기 전에 아래 순서로 확인합니다.

```text
npm run test:mv
```

통과하면 개선을 진행합니다.  
개선 후 다시 같은 명령을 실행해서 결과가 계속 `PASS`인지 확인합니다.

## 관련 파일

- `package.json`: `test:mv` 명령 정의
- `tests/run_mv_smoke_tests.js`: MV 통합 테스트 러너
- `tests/mv_*.js`: MV 개별 보호 테스트
- `MV_개선작업_이력.md`: 개선 작업 기록
- `MV_중복함수_정리계획.md`: 중복 함수 정리 계획
- `프로젝트_진단_보고서_2026-05-06.md`: 초기 진단 보고서
