# MV 2차 고도화 로드맵

작성일: 2026-05-06

## 1. 출발 기준

MV 안정화 1차 작업은 완료됐습니다.

```text
릴리스 태그: mv-stabilization-2026-05-06
최신 릴리스 노트 커밋: 8b40950 Add MV stabilization release notes
자동 테스트: npm run test:mv -> PASS (89 checks)
현재 판정: 릴리스 가능
```

2차 고도화는 구조 안정화가 아니라 실제 제작 효율과 결과물 품질을 높이는 방향으로 진행합니다.

## 2. 2차 고도화 목표

1. MV 씬을 더 빠르게 만들고 고치는 편집 경험 개선
2. Suno, 이미지 생성기, 영상 생성기 사이의 복사/내보내기 단계 축소
3. 장기 프로젝트에서 저장, 복원, 버전 관리 불안 감소
4. 실제 뮤직비디오 제작에 필요한 프롬프트 품질 점검 자동화 확대

## 3. 추천 진행 순서

### 1단계: MV 작업 속도 개선

사용자가 가장 자주 반복하는 동작을 줄입니다.

1. 완료: 씬별 EN/KO 프롬프트 변경 시 `미저장` 표시 추가
2. 완료: MV 탭 안에서 `Ctrl+S`/`Cmd+S`를 현재 편집 씬 저장과 연결
3. 씬 카드 안에서 `저장`, `복사`, `재생성` 버튼 상태를 더 명확히 표시
4. 씬 전체 저장 버튼과 개별 씬 저장 버튼의 역할 구분 문구 정리

검증 기준:

```text
npm run test:mv
```

### 2단계: MV 내보내기 품질 강화

생성된 씬 프롬프트를 외부 도구에 옮기는 시간을 줄입니다.

1. 이미지 생성용 전체 프롬프트 묶음 내보내기 형식 추가
2. 완료: Runway/Pika/Kling 등 영상 생성 도구별 프롬프트 템플릿 분리
3. 씬 번호, 시간, 가사, EN 프롬프트, KO 설명을 표 형태로 복사하는 기능 추가
4. TXT/SRT 내보내기 결과에 프로젝트 제목과 릴리스 기준 정보 포함

검증 기준:

```text
node tests/mv_download_prompts_smoke.js
node tests/mv_srt_export_smoke.js
npm run test:mv
```

### 3단계: 실제 프로젝트 장기 작업 보호

긴 작업 중 실수로 데이터가 흐트러지는 상황을 줄입니다.

1. 실제 장기 작업 프로젝트 1개로 운영 전 리허설 완료
2. 프로젝트 저장 전 `marketing.mv` 데이터 요약 로그 또는 진단 버튼 추가
3. 저장 직전/직후 씬 개수, 첫 씬, 마지막 씬, 수정 시각 비교 표시
4. 단일 프로젝트 JSON 내보내기/가져오기 흐름 정리

검증 기준:

```text
npm run rehearse:mv
```

### 4단계: 프롬프트 품질 자동 점검 확대

씬 프롬프트가 영상 제작에 바로 쓰일 수 있는지 점검합니다.

1. 프롬프트 길이, 금지어, 중복 표현, 비어 있는 장소/카메라 값 표시
2. 씬 간 배경/구도/카메라가 너무 반복될 때 경고
3. 인물 일관성 키워드 누락 시 표시
4. 품질 요약을 내보내기 전 최종 체크리스트로 확장

검증 기준:

```text
node tests/mv_confirm_scene_overview_smoke.js
npm run test:mv
```

## 4. 우선순위 제안

| 우선순위 | 작업 | 이유 |
|---:|---|---|
| 1 | 실제 장기 작업 프로젝트 리허설 | 안정화 결과를 실제 데이터로 확인 |
| 2 | 미저장 표시와 저장 단축키 | 반복 편집 중 실수 감소 |
| 3 | 완료: 영상 생성 도구별 내보내기 템플릿 | MV 제작 프로그램으로서 체감 효과 큼 |
| 4 | 단일 프로젝트 JSON 내보내기 | 장기 작업 백업 안정성 강화 |
| 5 | 프롬프트 품질 자동 점검 확대 | 결과물 품질 향상 |

## 5. 다음 실행 후보

가장 안전한 다음 작업은 아래 둘 중 하나입니다.

1. 실제 장기 작업 프로젝트 1개로 `MV_운영전_리허설_체크리스트.md` 수행
2. 씬 번호, 시간, 가사, EN 프롬프트, KO 설명을 표 형태로 복사하는 기능 추가

실제 사용자 프로젝트를 바로 선택할 수 없는 경우에는 2번부터 진행하는 것이 좋습니다.
