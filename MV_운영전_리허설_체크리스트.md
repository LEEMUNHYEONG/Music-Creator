# MV 운영 전 리허설 체크리스트

작성일: 2026-05-06

이 문서는 MV 6단계 개선 작업을 실제 사용자 데이터로 운영하기 전에 확인할 리허설 절차입니다.  
자동 보호 테스트는 기능 회귀를 막고, 아래 수동 리허설은 실제 화면에서 작업 감각과 결과물 품질을 확인하는 용도입니다.

## 1. 시작 전 안전 확인

1. 현재 브랜치 확인
   ```text
   git branch --show-current
   ```
   기대값: `codex/mv-data-model-stabilize`

2. MV 보호 테스트 실행
   ```text
   npm run test:mv
   ```
   기대값: `MV smoke test suite: PASS (99 checks)`

3. 자동 테스트 통과 후 리허설 서버까지 한 번에 실행하려면 아래 명령을 사용합니다.
   ```text
   npm run rehearse:mv
   ```

4. 기존 백업 위치 확인
   ```text
   /Users/leemunhyeong/Music Creator Backups/backup_2026-05-06_before_mv_improvements
   ```

## 2. 실제 사용자 데이터 리허설 순서

1. 앱을 실행하고 기존 프로젝트 하나를 엽니다.
   ```text
   npm run rehearse:mv
   ```
   앱 주소: `http://127.0.0.1:4180/index.html`
2. 6단계 마케팅 자료 화면으로 이동합니다.
3. `MV 프롬프트` 탭을 엽니다.
4. MV 설정을 확인합니다.
   - 노래 길이
   - 장면 전환 간격
   - 시대/지역
   - 조명/카메라/무드
   - 장소 태그와 직접 입력
   - 인물 정보와 캐릭터 시트

5. 씬 개요 편집 화면에서 최소 2개 씬을 직접 수정합니다.
   - 시작/종료 시간
   - 가사 구간
   - 장소
   - 감정
   - 무드
   - 조명
   - 카메라
   - 영어/한글 프롬프트

6. 씬 품질 요약을 확인합니다.
   - 확인 필요 항목이 있으면 필터 버튼으로 해당 씬에 이동
   - 시간 역전, 빈 메타데이터, 빈 프롬프트가 의도한 상태인지 확인

7. 개별 씬 카드의 `씬 저장`과 하단의 `현재 편집 내용 전체 저장` 역할 차이를 확인합니다.
   - `씬 저장`: 해당 씬의 EN/KO 프롬프트만 저장
   - `현재 편집 내용 전체 저장`: 전체 MV 설정·씬 목록·프롬프트 묶음을 프로젝트에 확정 저장
   - 복사 버튼은 저장과 별개로 클립보드 전송만 수행
   - 재생성 중/완료/오류 상태가 씬 카드 상태 라인에 표시되는지 확인
8. `현재 편집 내용 전체 저장`을 누릅니다.
9. 확인 알림이 뜨는 경우 메시지가 이해되는지 확인합니다.
   - 취소 시 첫 문제 씬으로 이동
   - 확인 시 결과 화면으로 이동

10. 결과 화면에서 확인합니다.
   - 씬 수가 맞는지
   - 씬별 EN/KO 프롬프트가 유지되는지
   - 타임라인/메타데이터가 보이는지
   - 복사/TXT/SRT 내보내기 결과에 씬 정보가 포함되는지
   - `MV 진단` 버튼을 눌러 `MV 실제 프로젝트 리허설 판정`이 `통과`인지 확인

11. 프로젝트를 저장한 뒤 앱을 새로고침하거나 프로젝트를 다시 엽니다.
12. 6단계로 재진입해 다음 값이 유지되는지 확인합니다.
    - MV 설정
    - 공통 썸네일/배경/인물 프롬프트
    - 씬별 시간/가사/메타데이터
    - 씬별 EN/KO 프롬프트
    - 결과 화면 표시 상태

## 3. 통과 기준

리허설은 아래 조건을 모두 만족하면 통과로 봅니다.

1. `npm run test:mv`가 `PASS (99 checks)`로 끝납니다.
2. 실제 프로젝트에서 MV 6단계 화면이 열립니다.
3. 씬 편집값을 저장/확정해도 데이터가 사라지지 않습니다.
4. 프로젝트 재진입 후 `marketing.mv` 기준 데이터가 복원됩니다.
5. 결과 화면의 복사/TXT/SRT 출력에 씬별 타임라인과 메타데이터가 포함됩니다.
6. 모바일 폭에서도 씬 카드가 가로로 깨지지 않습니다.

## 4. 문제 발생 시 되돌리는 기준

1. 자동 테스트 실패 시 마지막 실패 테스트 파일명을 먼저 확인합니다.
2. 실제 화면에서만 실패하면 이 문서의 리허설 단계 번호를 기록합니다.
3. 최근 성공 커밋으로 되돌릴 수 있는 기준점:
   - `3116aff Record MV stabilization release closeout`
   - `dd33517 Fix MV scene prompt restore persistence`
4. 필요 시 전체 백업 폴더에서 원본을 비교합니다.

## 5. 이번 리허설 자동 검증 결과

2026-05-06 기준 자동 리허설 결과:

```text
npm run test:mv
MV smoke test suite: PASS (85 checks, 당시 기준)
```

2026-05-06 11:35 KST 기준으로 샘플 프로젝트 `Codex MV 리허설 샘플`에서는 MV 탭 진입, API 키 없음 로컬 씬 생성, 씬 5 편집 저장, 새로고침 후 프로젝트 재로드 복원까지 통과했습니다.

2026-05-07 03:23 KST 기준 자동 리허설 보강 결과:

```text
node tests/mv_preflight_rehearsal_runtime_check.js
MV preflight rehearsal runtime check: PASS

npm run test:mv
MV smoke test suite: PASS (99 checks)
```

추가 확인 범위:

- 샘플 프로젝트 복원 후 MV 탭 진입
- 씬 카드 작업 상태 라인 렌더링
- `씬 저장` 버튼 문구와 역할 안내
- 복사 버튼의 저장 별개 안내
- 재생성 버튼의 다시 생성 안내

2026-05-07 최신 자동 리허설 보강 결과:

```text
node tests/mv_preflight_rehearsal_runtime_check.js
MV preflight rehearsal runtime check: PASS

npm run test:mv
MV smoke test suite: PASS (99 checks)
```

추가 확인 범위:

- 대표 프로젝트 복원 후 품질 요약의 `전체/준비 완료/확인 필요` 개수 표시
- `확인 필요만 보기` 토글 시 준비 완료 씬 숨김과 버튼 문구 변경
- 전체 MV 프롬프트 최종 확인 메시지의 프로젝트명, 씬 수, 포함 항목 표시

2026-05-07 실제 프로젝트 리허설 판정 보강 결과:

```text
node tests/mv_marketing_diagnostics_smoke.js
MV marketing diagnostics smoke test: PASS

npm run test:mv
MV smoke test suite: PASS (99 checks)
```

추가 확인 범위:

- `MV 진단` 메시지에 `MV 실제 프로젝트 리허설 판정` 섹션 표시
- 씬 데이터, canonical/legacy 씬 수 동기화, 씬별 프롬프트 누락, MV 설정, 공통 프롬프트 저장 여부를 `통과/보류/실패`로 표시
- 진단 메시지의 `다음 조치` 확인
- 진단 보고서가 클립보드에 복사되어 기록지에 붙여넣을 수 있는지 확인

운영 전 남은 확인은 실제 사용자가 선택한 장기 작업 프로젝트 1개로 위 수동 리허설을 한 번 수행하는 것입니다.
