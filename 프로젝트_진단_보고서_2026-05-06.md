# Music Creator 프로젝트 진단 보고서

작성일: 2026-05-06  
대상 경로: `/Users/leemunhyeong/Music Creator`

## 1. 총평

현재 프로젝트는 단순한 뮤직비디오 제작기가 아니라, **가사 작성 → Suno용 변환 → AI 분석 → 개선안 적용 → 최종 출력 → 마케팅/MV 제작 자료 생성**까지 이어지는 통합 제작 보조 도구입니다.

특히 6단계에는 뮤직비디오 제작을 위한 핵심 재료가 이미 많이 구현되어 있습니다. 노래 길이와 장면 전환 간격에 따른 이미지 수 계산, MV 상세 설정, 캐릭터 설정, 썸네일/배경/인물 프롬프트, 씬별 프롬프트, SRT 자막, 업로드 체크리스트가 포함되어 있습니다.

다만 현재 상태는 “완성된 영상 편집 프로그램”이라기보다 **AI 음악/MV 제작 프롬프트 워크스테이션**에 가깝습니다. 실제 MV 제작 프로그램으로 발전시키려면 타임라인, 컷/씬 관리, 음원-가사 싱크, 생성 이미지/영상 파일 관리, 최종 내보내기 파이프라인이 다음 핵심 과제입니다.

## 2. 현재 구조

### 기술 스택

- 프론트엔드: HTML, CSS, Vanilla JavaScript
- 인증/DB/호스팅: Firebase Auth, Firestore, Storage, Hosting
- 백엔드: Firebase Cloud Functions v2
- AI 연동: Gemini API, OpenAI API
- 저장: localStorage + Firestore 자동 백업

### 주요 파일

| 파일 | 역할 | 진단 |
| --- | --- | --- |
| `index.html` | 전체 화면, 단계별 UI, 모달, 인라인 스크립트 | 기능이 많고 인라인 로직이 과다합니다. |
| `app.js` | 전역 상태, 단계 이동, 저장, AI 호출, UI 제어 | 9,602줄로 가장 큰 유지보수 위험입니다. |
| `styles.css` | 전체 스타일 | 3,866줄로 크지만 분리 가능성이 있습니다. |
| `js/step1.js` ~ `js/step6.js` | 단계별 기능 분리 시도 | 좋은 방향이나 `app.js`와 중복이 아직 남아 있습니다. |
| `js/storage.js` | 프로젝트 저장/로드, Firestore 백업 | 핵심 데이터 보존 로직입니다. |
| `functions/index.js` | OpenAI/Gemini 보안 프록시 | 방향은 좋지만 프론트가 아직 직접 API 키를 쓰는 부분이 많습니다. |
| `firestore.rules`, `storage.rules` | 권한 규칙 | 기본 구조는 있으나 Storage 삭제 권한은 더 좁힐 필요가 있습니다. |

## 3. 현재 구현된 핵심 기능

### 제작 워크플로우

- 1단계: 가사 작성 또는 AI 가사 생성
- 2단계: Suno용 가사/스타일 프롬프트 변환
- 3단계: Gemini 기반 가사 분석
- 4단계: 개선안 선택 및 적용
- 5단계: 최종 가사/스타일 출력 및 평가
- 6단계: 마케팅 자료, 썸네일 문구, MV 프롬프트, SRT, 업로드 가이드 생성

### MV 관련 기능

- 노래 길이와 이미지당 초를 기준으로 예상 이미지 수 계산
- 시대, 국가, 장소, 조명, 카메라워크, 분위기 설정
- 인물 수와 인물 외형/스타일 설정
- 씬별 이미지 프롬프트 생성
- 썸네일/배경/인물 통합 프롬프트 생성
- 영어/한국어 프롬프트 동기화
- 모든 MV 프롬프트 복사 및 TXT 다운로드
- SRT 자막 생성/복사/다운로드
- 음원 파일 업로드 UI와 자동 MV 제작 안내 UI
- 제작 체크리스트

## 4. 강점

### 4.1 제작 흐름이 실제 창작 과정에 맞춰져 있음

가사와 Suno 프롬프트만 다루는 도구가 아니라, 최종 업로드 자료와 MV 제작 준비까지 연결되어 있습니다. 이 구조는 음악 창작자가 실제로 반복하는 작업을 잘 반영합니다.

### 4.2 MV 프롬프트 기능의 밀도가 높음

`js/step6.js`에는 씬 생성, 썸네일/배경/캐릭터 프롬프트, 프롬프트 재생성, 번역, 복사, 저장 흐름이 들어 있습니다. 이 부분은 앞으로 제품의 차별화 지점이 될 가능성이 큽니다.

### 4.3 Firebase 기반 사용자/클라우드 저장 구조가 있음

로컬 저장만이 아니라 Firestore 자동 백업과 사용자 인증 구조가 있습니다. 개인용 도구에서 팀/회원형 서비스로 확장할 수 있는 기반입니다.

### 4.4 API 키 보안 프록시 방향이 잡혀 있음

`functions/index.js`에 OpenAI/Gemini 프록시가 있고, 승인된 사용자만 호출하도록 `verifyApprovedUser`가 들어 있습니다. 방향은 좋습니다.

## 5. 주요 리스크

### 5.1 `app.js`와 `index.html` 비대화

현재 코드 규모는 다음과 같습니다.

| 파일 | 줄 수 |
| --- | ---: |
| `app.js` | 9,602 |
| `index.html` | 6,687 |
| `styles.css` | 3,866 |
| `js/step6.js` | 4,148 |

`app.js`와 `index.html`에 핵심 기능이 너무 많이 남아 있어, 작은 수정도 다른 단계나 모달에 영향을 줄 수 있습니다. 특히 `index.html` 안에도 임시 로드 스크립트, fallback 함수, 클라우드 동기화 로직이 섞여 있습니다.

### 5.2 중복 함수와 로드 순서 위험

`index.html`은 `js/step6.js`를 먼저 로드하고 마지막에 `app.js`를 로드합니다. 그런데 MV 관련 함수가 `app.js`와 `js/step6.js` 양쪽에 존재합니다.

예를 들어 다음 기능군이 중복 또는 과거 구현 흔적으로 보입니다.

- `generateMVDetailPrompts`
- `copyAllMVPrompts`
- `syncMVPromptTranslation`
- `regenerateMVPrompt`
- `saveMVSettings`
- `updateMVImageCount`
- `getMVLocationValues`

마지막에 로드되는 `app.js`가 일부 함수를 덮어쓸 수 있으므로, 실제 브라우저에서 어떤 구현이 실행되는지 추적하기 어려워집니다.

### 5.3 프론트에서 API 키 직접 사용이 아직 많음

Cloud Functions 프록시가 있지만, 프론트 코드에는 여전히 `localStorage`의 `openai_api_key`, `gemini_api_key`를 읽어 직접 외부 API를 호출하는 코드가 많습니다.

이 구조는 개인용 로컬 실행에서는 편하지만, 배포형 서비스에서는 다음 문제가 생깁니다.

- 브라우저에 개인 API 키가 저장됨
- 공용 키를 프론트 설정으로 노출할 위험
- 호출 정책, 사용량 제한, 로그 관리가 어려움
- 승인 사용자 검증이 우회될 수 있음

### 5.4 저장 구조가 여러 저장소에 분산됨

프로젝트 데이터가 `musicCreatorProjects`, `savedProjects`, 여러 히스토리 키, `mvSettings`, Firestore에 나뉘어 저장됩니다. 호환성 측면에서는 장점이 있지만, 장기적으로는 데이터 소스 충돌과 복원 오류를 만들 수 있습니다.

특히 MV 설정은 프로젝트 내부 `marketing.mvSettings`와 전역 `localStorage.mvSettings`가 같이 존재합니다. 프로젝트별 설정과 전역 설정의 경계를 명확히 해야 합니다.

### 5.5 실제 MV 편집 기능은 아직 외부 도구 의존

현재 6단계는 Midjourney/Runway/CapCut/Final Cut 등 외부 도구에 넣을 자료를 생성하는 구조입니다. 아직 앱 안에서 다음 기능은 부족합니다.

- 영상/이미지 에셋 라이브러리
- 타임라인 편집
- 컷 길이 조정
- 음원 파형 표시
- 가사 싱크 편집
- 이미지/영상 프리뷰
- MP4 내보내기

따라서 제품 포지셔닝은 당분간 “MV 제작 프로그램”보다 “AI MV 제작 준비 도구”로 잡는 것이 현실적입니다.

### 5.6 테스트와 품질 게이트가 약함

확인 결과:

- `node --check app.js`: 통과
- `node --check js/step6.js`: 통과
- `node --check functions/index.js`: 통과
- `npm --prefix functions run lint`: 실행은 되지만 실제 lint가 아니라 `echo "No linting configured"`만 수행

즉 문법 오류는 없지만, 자동 테스트/린트/브라우저 회귀 검증은 아직 부족합니다.

### 5.7 Storage 삭제 권한이 넓음

`storage.rules`에서 `board_images` 삭제가 로그인 사용자 전체에게 열려 있습니다.

```text
allow delete: if request.auth != null;
```

게시판 이미지가 사용자별 소유권을 갖는다면, 삭제 권한은 작성자 또는 관리자만 가능하도록 좁히는 것이 좋습니다.

## 6. MV 제작 프로그램으로 발전시키기 위한 우선순위

### 1순위: MV 데이터 모델 정리

현재 `marketing.mvScenes`, `marketing.mvPrompts`, `window.currentScenes`, DOM textarea 값이 섞여 있습니다. 먼저 MV 전용 데이터 모델을 고정해야 합니다.

권장 구조:

```javascript
marketing: {
  mv: {
    settings: {},
    characters: [],
    scenes: [
      {
        id,
        index,
        startTime,
        endTime,
        lyricText,
        sceneSummaryKo,
        imagePromptEn,
        imagePromptKo,
        videoPromptEn,
        videoPromptKo,
        assetRefs: []
      }
    ],
    subtitles: [],
    exports: []
  }
}
```

효과:

- 씬 저장/복원이 안정됨
- SRT와 MV 프롬프트가 같은 시간 기준을 공유함
- 나중에 타임라인과 export 기능을 붙이기 쉬움

### 2순위: `step6.js`를 MV 기능의 단일 소유자로 만들기

MV 관련 함수는 `js/step6.js`로 모으고, `app.js`에서는 단계 전환과 공통 기능만 담당하게 하는 것이 좋습니다.

정리 대상:

- `generateSceneOverview`
- `generateMVThumbnailPrompts`
- `saveMVSettings`
- `loadMVSettings`
- `updateMVImageCount`
- `copyAllMVPrompts`
- `downloadMVPrompts`
- `syncMVPromptTranslation`
- `regenerateMVPrompt`

### 3순위: API 호출을 Cloud Functions 프록시로 통일

프론트에서 직접 Gemini/OpenAI를 호출하는 방식을 줄이고, `/api/chat`, `/api/gemini` 또는 전용 Functions 엔드포인트로 통일하는 것이 좋습니다.

효과:

- API 키 노출 최소화
- 사용자 승인/권한 제어 일관성 확보
- 사용량 로깅/제한 가능
- 모델 교체가 쉬워짐

### 4순위: 가사 기반 씬 분할 고도화

현재는 노래 길이와 이미지 간격 중심으로 이미지 수를 계산합니다. MV 제작 도구답게 만들려면 가사 구조를 기준으로 씬을 나누는 기능이 중요합니다.

권장 기능:

- Verse/Chorus/Bridge 자동 감지
- 후렴 반복 구간 시각 테마 유지
- 감정 변화에 따른 색감/카메라워크 변경
- 씬별 가사 줄 연결
- 시작/종료 시간 자동 배치

### 5순위: 프리뷰 중심 UI 추가

현재 프롬프트 텍스트 중심 UI가 강합니다. 다음 단계에서는 “영상 제작자가 보는 화면”에 가까운 프리뷰가 필요합니다.

권장 UI:

- 씬 카드 리스트
- 타임코드 표시
- 장면별 가사/프롬프트/자막 동시 보기
- 씬 순서 드래그
- 컷 길이 조정
- 이미지/영상 결과 업로드 및 미리보기

### 6순위: 품질 게이트 추가

최소한 다음을 추가하는 것이 좋습니다.

- ESLint 또는 Biome
- Prettier
- 핵심 저장/복원 단위 테스트
- MV 씬 생성 결과 JSON 파서 테스트
- Firebase Functions 테스트
- 로컬 브라우저 smoke test

## 7. 추천 개발 로드맵

### 단기: 1~2주

1. MV 관련 중복 함수 정리
2. `step6.js`를 MV 기능 단일 진입점으로 정리
3. MV 데이터 모델 확정
4. 저장/복원 버그 검증
5. API 호출 경로 목록화

### 중기: 3~6주

1. 가사 기반 씬 분할 기능 추가
2. 씬별 타임코드 생성
3. SRT와 MV 씬 시간 기준 통합
4. 씬 카드/프리뷰 UI 추가
5. 생성 이미지/영상 파일 업로드 및 씬 연결

### 장기: 2~3개월

1. 타임라인 편집 UI
2. 오디오 파형 표시
3. 영상/이미지 프리뷰 플레이어
4. ffmpeg 또는 외부 렌더링 파이프라인 연동
5. MP4 내보내기 또는 편집툴용 패키지 export

## 8. 권장 제품 방향

현재 프로젝트에 가장 자연스러운 포지션은 다음입니다.

> Suno로 만든 음악을 YouTube/TikTok용 콘텐츠로 완성하기 위한 AI 제작 보조 도구

이 방향이라면 앱의 강점이 명확합니다.

- 가사와 스타일 프롬프트를 만든다.
- AI가 가사를 분석하고 개선한다.
- 최종 업로드 설명과 해시태그를 만든다.
- MV 장면 프롬프트와 자막을 만든다.
- 제작 체크리스트로 실제 업로드까지 안내한다.

반대로 “앱 안에서 모든 영상을 직접 편집하고 렌더링하는 프로그램”으로 바로 가면 개발 범위가 급격히 커집니다. 우선은 **MV 제작 준비와 에셋 관리**를 강하게 만든 뒤, 나중에 타임라인/렌더링으로 확장하는 순서가 안전합니다.

## 9. 다음 작업 제안

가장 먼저 추천하는 실제 개발 작업은 다음 중 하나입니다.

1. `step6.js` 중심으로 MV 함수 중복 제거
2. MV 데이터 모델 정리 및 저장/복원 안정화
3. 가사 기반 씬 분할 + 타임코드 생성 기능 추가
4. API 직접 호출을 Cloud Functions 프록시로 통일
5. 6단계 UI를 “씬 카드 + 타임라인 준비 화면”으로 재구성

우선순위는 **2번 MV 데이터 모델 정리 → 1번 중복 제거 → 3번 씬 분할 고도화** 순서를 권장합니다. 이 순서로 가면 기존 기능을 망가뜨리지 않고 MV 제작 프로그램의 뼈대를 단단하게 만들 수 있습니다.
