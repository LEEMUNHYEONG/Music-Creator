# MV 릴리스 노트 2026-05-06

## 릴리스 기준

```text
릴리스 태그: mv-stabilization-2026-05-06
브랜치: codex/mv-data-model-stabilize
기준 커밋: 70e7b64 Record MV stabilization release tag
자동 테스트: npm run test:mv -> PASS (85 checks)
판정: 릴리스 가능
```

## 주요 개선

1. MV 6단계 함수 소유권을 `js/step6.js` 중심으로 정리했습니다.
2. `marketing.mv` 정규화 모델을 기준으로 기존 `mvSettings`, `mvPrompts`, `mvScenes` 호환을 유지했습니다.
3. 6단계 복원, 씬 카드, 타임라인, 메타데이터, SRT, 복사/TXT 내보내기 흐름을 보호 테스트로 묶었습니다.
4. 씬별 시간, 가사, 장소, 감정, 무드, 조명, 카메라, EN/KO 프롬프트 편집값 저장/복원을 보강했습니다.
5. Gemini API 키가 없어도 MV 프롬프트 탭 작업공간을 열 수 있게 했습니다.
6. API 키 없이도 로컬 기본 방식으로 MV 씬 카드를 생성할 수 있게 했습니다.
7. 프로젝트 복원 중 객체형 보컬 파트 지정값으로 렌더링이 중단되는 문제를 방지했습니다.
8. 씬 개별 저장값이 새로고침과 프로젝트 재로드 후에도 유지되도록 `mvScenes`와 `marketing.mv.scenes` 동시 갱신을 보강했습니다.

## 실제 화면 리허설 결과

샘플 프로젝트 `Codex MV 리허설 샘플` 기준으로 아래 흐름을 확인했습니다.

1. 앱 로드와 저장 프로젝트 목록 표시
2. 1단계에서 2단계 이동
3. 진행바 6단계 진입
4. Gemini API 키 없음 상태에서 MV 프롬프트 탭 진입
5. API 키 없이 27개 씬 카드 생성
6. 씬 5 영어 프롬프트 편집 및 개별 저장
7. 새로고침 후 최신 샘플 프로젝트 재로드
8. 씬 5 편집 프롬프트 복원 확인

## 검증 명령

```text
cd "/Users/leemunhyeong/Music Creator"
npm run test:mv
```

정상 결과:

```text
MV smoke test suite: PASS (85 checks)
```

수동 리허설:

```text
cd "/Users/leemunhyeong/Music Creator"
npm run rehearse:mv
```

앱 주소:

```text
http://127.0.0.1:4180/index.html
```

## 되돌림 기준

운영 기준 태그:

```text
mv-stabilization-2026-05-06
```

주요 기준 커밋:

```text
70e7b64 Record MV stabilization release tag
01df28e Add MV operations handoff summary
6edb6f3 Refresh MV rehearsal documentation baseline
98acbe6 Update MV rehearsal checklist baseline
3116aff Record MV stabilization release closeout
dd33517 Fix MV scene prompt restore persistence
```

원본 백업:

```text
/Users/leemunhyeong/Music Creator Backups/backup_2026-05-06_before_mv_improvements
```

## 남은 권장 확인

릴리스 차단 항목은 없습니다.

운영 전 또는 장시간 사용 전에는 실제 장기 작업 프로젝트 1개로 아래 항목을 추가 확인하는 것을 권장합니다.

1. 기존 프로젝트 열기
2. 6단계 MV 프롬프트 탭 진입
3. 씬 2개 이상 편집
4. 저장/확정
5. 새로고침 후 프로젝트 재로드
6. MV 설정과 씬별 편집값 유지 확인
7. 복사/TXT/SRT 내보내기 결과 확인

상세 절차는 `MV_운영전_리허설_체크리스트.md`를 기준으로 합니다.
